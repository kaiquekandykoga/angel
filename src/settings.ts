/** The login the bot comments as; its comments are what "already reviewed" means. */
export const REVIEWER_LOGIN = "kandy-angel[bot]";

/** Only pull requests and issues carrying this label are reviewed. */
export const LABEL = "angel";

/** Hex color the label is created with when a repository lacks it. */
export const LABEL_COLOR = "f709c2";
